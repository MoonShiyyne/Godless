namespace Godless.Sim.Headless
{
    /// <summary>
    /// Binds tests and tooling to this assembly without depending on a real
    /// system. Delete once any stratum-0 system exists to reference instead.
    /// </summary>
    public static class AssemblyMarker
    {
    }
}
